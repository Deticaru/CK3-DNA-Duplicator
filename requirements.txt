# Dependencies for DNA Duplicator
# Note: For Python 3.14, install pywebview with: pip install --no-deps pywebview
# Then install: pip install bottle proxy_tools typing_extensions
# (pythonnet is optional - pywebview will use alternative backends on Windows)

pywebview>=6.0
bottle>=0.13.4
proxy_tools>=0.1.0
typing_extensions>=4.15.0
